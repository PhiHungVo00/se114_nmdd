<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- 
      Base application theme.
      Theme.MyApplication kế thừa từ Theme.MaterialComponents.DayNight.DarkActionBar 
      (hỗ trợ tự động chuyển dark/light mode, dùng ActionBar tối).
    -->
    <style name="Theme.MyApplication" parent="Theme.MaterialComponents.DayNight.DarkActionBar">

        <!-- Ẩn ActionBar mặc định và bỏ tiêu đề cửa sổ để tự tùy biến giao diện -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/black</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Status bar color: màu thanh trạng thái trên cùng màn hình -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        
        <!-- Customize your theme here. (có thể bổ sung thêm thuộc tính tuỳ biến) -->
    </style>
</resources>
